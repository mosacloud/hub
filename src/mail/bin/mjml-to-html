#!/usr/bin/env bash

# Run mjml command to convert all mjml templates to html files
HUB_DIR_MAILS="${HUB_DIR_MAILS:-../backend/core/templates/mail}/html/"

if [ ! -d "${HUB_DIR_MAILS}" ]; then
  mkdir -p "${HUB_DIR_MAILS}";
fi
mjml mjml/*.mjml -o "${HUB_DIR_MAILS}";
